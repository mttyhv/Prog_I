/*typedef struct {
    double* v;
    int tamanho;
} Vetor;
*/
double* merge_vectors(double v[], int n, double w[], int m);
//Vetor merge_vectors(Vetor v, Vetor w);
double* sort_vector(double u[], int o);
//Vetor sort_vector* (Vetor u);
